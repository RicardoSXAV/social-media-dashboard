import { ToggleCircle, ToggleContainer } from "./styles";

const Toggle: React.FC = () => {
  return (
    <ToggleContainer>
      <ToggleCircle />
    </ToggleContainer>
  );
};

export default Toggle;
